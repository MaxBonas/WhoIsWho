<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />


    <application
            android:name=".MyApp"
            android:allowBackup="true"
            android:fullBackupContent="@xml/backup_rules"
            tools:targetApi="31"
            android:dataExtractionRules="@xml/data_extraction_rules"
            android:label="@string/app_name"
            android:icon="@mipmap/ic_launcher"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.FaceTrace">


        <!-- Definición de la actividad de Splash -->
        <activity android:name=".SplashActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".IntroActivity" />

        <!-- Definición de la actividad del título del juego -->
        <activity android:name=".TitleActivity" android:exported="false" />

        <!-- Definición de la actividad del menú principal -->
        <activity android:name=".MainMenuActivity" android:exported="false" />

        <!-- Definición de la actividad de selección de dificultad -->
        <activity android:name=".DifficultySelectionActivity" android:exported="false" />

        <!-- Definición de la actividad de tutorial -->
        <activity android:name=".TutorialActivity" />

        <!-- Definición de la actividad principal del juego (si todavía no la has agregado) -->
        <activity android:name=".MainActivity" android:exported="false" />

        <activity android:name=".LocalPvPActivity" android:exported="false" />

        <activity android:name=".PvPSelectionActivity" android:exported="false" />

        <activity android:name=".ScoresActivity" />

        <activity android:name=".LanguageSelectionActivity" />

    </application>

</manifest>
